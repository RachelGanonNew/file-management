import { defineMessages } from 'react-intl';

export const scope = 'app.components.Png';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage:"png component",
  },
});
