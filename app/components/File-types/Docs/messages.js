import { defineMessages } from 'react-intl';

export const scope = 'app.components.Docs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "docs component",
  },
});
