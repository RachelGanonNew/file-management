
import { defineMessages } from 'react-intl';

export const scope = 'app.components.Pdf';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage:"pdf component",
  },
});
