import { defineMessages } from 'react-intl';

export const scope = 'app.components.Jpg';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "jpg component",
  },
});
