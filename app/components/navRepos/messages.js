import { defineMessages } from 'react-intl';

export const scope = 'app.components.Folder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "folder component!",
  },
});
