/*
 * AddFolder Messages
 *
 * This contains all the text for the AddFolder component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddFolder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddFolder component!',
  },
});
