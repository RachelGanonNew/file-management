/*
 * TableSummarizing Messages
 *
 * This contains all the text for the TableSummarizing container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableSummarizing';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableSummarizing container!',
  },
});
